# Danh sách Thành viên & Phân chia Nhiệm vụ Nhóm

| STT | Họ và Tên | MSSV | GitHub Username | Vai trò & Nhiệm vụ cụ thể (Tasks) |
|:---:|:---|:---:|:---|:---|
| 1 | **Nguyễn Ngọc Bảo** (Nhóm trưởng) | 2A202602951 | KeepGoing132 | **Task 1:** Thiết kế kiến trúc tổng thể, điều phối đánh giá run eval.<br>**Task 2:** Tối ưu hóa `system_prompt.md` qua 4 phiên bản (`v0` $\rightarrow$ `v3`) giải quyết routing boundaries, missing info handling và confirmation.<br>**Task 3:** Thiết kế 5 single-turn cases cho nhóm (`G01` - `G05` trong `eval_group.json`).<br>**Task 4:** Tổng hợp metrics, ghi `version_log.csv` và kiểm tra tính toàn vẹn artifact. |
| 2 | **Nguyễn Phú Bình** (Thành viên) | 2A202602410 | binhbadao05 | **Task 1:** Chuẩn hóa khai báo `tools.yaml` (bổ sung schema chi tiết, ràng buộc tham số, enums và ranh giới dữ liệu cho external tool).<br>**Task 2:** Thiết kế 5 multi-turn cases cho nhóm (`G06` - `G10` trong `eval_group.json` gồm correction, cancellation, carryover, stale confirmation).<br>**Task 3:** Phát triển giao diện Web Chat tương tác `app.py` bằng Streamlit kết nối với `chat.py` loop.<br>**Task 4:** Phân tích kiểm thử an toàn (`eval_adversarial.json`), rà soát chống rò rỉ credential và hoàn thiện báo cáo. |
